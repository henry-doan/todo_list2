<h1>The Task of <%= @item.name%> info:</h1>
<br />

<h2><%= @item.name %></h2>
	<p><%= @item.description %></p>
	<p><%= @item.completion %></p>

<%= link_to 'Edit item', edit_item_path(@item) %>

<%= link_to 'Delete', item_path(@item), method: :delete %>

<%= link_to 'All items on the list', items_path %>
